import type { EnvOptions } from "tnp/src";

const env: Partial<EnvOptions> = {
  website: {
    domain: "taon-containers.example.domain.com",
    title: "Taon Containers",
  },
};
export default env;
